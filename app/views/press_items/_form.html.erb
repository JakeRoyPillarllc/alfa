<%= form_for(@press_item) do |f| %>
  <% if @press_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@press_item.errors.count, "error") %> prohibited this press_item from being saved:</h2>

      <ul>
      <% @press_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :publication %><br />
    <%= f.text_field :publication %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
